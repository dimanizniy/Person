#pragma once

#include <iostream>
#include <vector>
#include <string>

#include "person.h"

using namespace std;

//void compare(vector <Person>& data_base);					// Сравнение двух человек
//void eliminate(vector <Person>& data_base);				// Удаление человека
//void birthday(vector <Person>& data_base);				// Кол-во дней до ДР
//void write(vector <Person>& data_base);					// Экспорт базы данных в файл
//void sortVector(vector <Person>& vec);					// Фукнция вызова сортировки

//bool elemsort(const Person& per1, const Person& per2);	// Функция сортировки